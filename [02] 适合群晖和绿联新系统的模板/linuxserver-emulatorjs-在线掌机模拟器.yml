# 官方文档
# https://emulatorjs.org
# https://hub.docker.com/r/linuxserver/emulatorjs
# https://docs.linuxserver.io/images/docker-emulatorjs

---
version: "3"
# 最后编辑时间：2024-03-11
services:
  emulatorjs:
    image: linuxserver/emulatorjs:latest
    # 镜像地址
    container_name: emulatorjs
    # 容器名字
    hostname: emulatorjs
    # 主机名
    volumes:
      - /volume1/docker/emulatorjs:/config
      # 配置目录
      - /volume1/docker/掌机游戏:/data
      # ROM目录
    environment:
      - SUBFOLDER=/
      # 自定义容器根路径，默认即可
      - PUID=1000
      # 用户权限，群晖用户一般写1026，unraid用户写1000，视情况写0，即root
      - PGID=100
      # 用户组权限
      - UMASK=022
      # 默认创建新文件的权限，一般写022
      - TZ=Asia/Shanghai
      # 时区
    network_mode: bridge
    # 80端口被占用，不能用host
    ports:
      - 13180:80/tcp
      # WebUI 端口
      - 4001:4001/tcp
      # IPFS 对等端口，如果您想参与 P2P 网络分发前端艺术品请将此转发到互联网
      - 3100:3000/tcp
      # Rom/艺术品管理界面，用于生成/管理配置文件和下载艺术品
    restart: unless-stopped
    # 重启策略，可根据实际情况而选择 no/always/unless-stopped/on-failure/on-failure:3
    labels:
      icon: http://IP:PORT/i/user_01/emulatorjs_A.png
      # 适用于CasaOS导入时自动写上图标地址
      # 注意：在导入CasaOS时，记得补全本机端口号
      # 注意：图标地址仅供参考，请根据实际填写，推荐自搭建兰空图床使用
      ########################################
      net.unraid.docker.managed: dockerman
      net.unraid.docker.webui: http://[IP]:[PORT:80]
      # 适用于unraid界面打开WebUI，注意端口号写的是容器端口，如有自定义，自行修改
      net.unraid.docker.icon: /mnt/user/LOGO/emulatorjs_A.png
      # 适用于unraid的图标，可以写unRAID的路径地址，也可以写一个图标地址(局域网或广域网均可)
      # 注意：通过compose创建的docker容器，无法在unRAID上进行编辑