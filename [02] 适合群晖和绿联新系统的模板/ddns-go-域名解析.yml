# 官方文档
# https://hub.docker.com/r/jeessy/ddns-go

---
version: "3"
# 最后编辑时间：2024-03-11
services:
  ddns-go:
    image: jeessy/ddns-go:latest
    # 镜像地址
    container_name: ddns-go
    # 容器名字
    hostname: ddns-go
    # 主机名
    command: -l :9876 -f 300
      # 9876为WebUI端口，可自定义
      # 300为刷新解析间隔时间，可自定义
    volumes:
      - /volume1/docker/ddns-go:/root
      # 配置目录，文件是隐藏的，需要通过ssh进行访问，很重要，切勿泄露
    # network_mode: bridge
    network_mode: host
    # 直接使用host模式，ipv6解析需要使用host网络模式
    # ports:
      # - 9876:9876/tcp
      # WebUI 端口
    restart: always
    # 重启策略，可根据实际情况而选择 no/always/unless-stopped/on-failure/on-failure:3
    labels:
      icon: http://IP:PORT/i/user_01/ddns-go_A.png
      # 适用于CasaOS导入时自动写上图标地址
      # 注意：在导入CasaOS时，记得补全本机端口号
      # 注意：图标地址仅供参考，请根据实际填写，推荐自搭建兰空图床使用
      ########################################
      net.unraid.docker.managed: dockerman
      net.unraid.docker.webui: http://[IP]:[PORT:9876]
      # 适用于unraid界面打开WebUI，注意端口号写的是容器端口，如有自定义，自行修改
      net.unraid.docker.icon: /mnt/user/LOGO/ddns-go.png
      # 适用于unraid的图标，可以写unRAID的路径地址，也可以写一个图标地址(局域网或广域网均可)
      # 注意：通过compose创建的docker容器，无法在unRAID上进行编辑