# 官方文档
# https://github.com/filebrowser/filebrowser
# https://hub.docker.com/r/filebrowser/filebrowser
# 部署教程
# https://github.com/filebrowser/filebrowser/blob/master/docs/installation.md
# https://post.smzdm.com/p/apvp2p2w

# 默认账号为 admin，密码看日志，存放在数据库当中

---
version: "3"
# 最后编辑时间：2025-10-29
services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    # 镜像地址
    container_name: filebrowser
    # 容器名字
    hostname: filebrowser
    # 主机名
    user: root
    # 赋予root权限
    volumes:
      - /volume1/docker/filebrowser/config:/config
      # 配置目录
      - /volume1/docker/filebrowser/database:/database
      # 数据库目录
      - /volume1/docker/filebrowser/srv:/srv
      - /volume1:/volume1
      # 映射本地其他路径使其可以访问，可以自定义多个，必须在/srv之下
    environment:
      - TZ=Asia/Shanghai
      # 时区
    network_mode: bridge
    # 80端口被占用，不能用host
    # 你可以选择在 /config/settings.json 中的80端口号为自定义容器端口，再使用host网络模式
    ports:
      - 3670:80
      # WebUI http端口
    restart: unless-stopped
    # 重启策略，可根据实际情况而选择 no/always/unless-stopped/on-failure/on-failure:3
